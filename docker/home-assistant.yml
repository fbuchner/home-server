name: home-assistant

services:
  home-assistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: home-assistant
    volumes:
      - /usr/media/misc/home-assistant:/config
    network_mode: host
    privileged: true
    restart: unless-stopped
